import React from 'react'

function LogIn(props) {
    return (
        <div>
            <button onClick={()=>{props.setscene('Map1')}}>dskjflkdsa</button>
        </div>
    )
}

export default LogIn
